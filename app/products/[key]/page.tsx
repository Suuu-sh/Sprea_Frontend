"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {AppShell} from "@/components/app-shell";import {PageHeader} from "@/components/ui";import {ProductHistory,ProductIdentity} from "@/components/pages/product-detail";import {getProductDetail,ProductDetail} from "@/lib/api";
export default function ProductPage(){const params=useParams<{key:string}>(),[d,setD]=useState<ProductDetail|null>(null);useEffect(()=>{getProductDetail(decodeURIComponent(params.key)).then(setD)},[params.key]);if(!d)return <AppShell><div className="table-message">商品履歴を読み込んでいます</div></AppShell>;return <AppShell><PageHeader title={d.title} description={d.canonicalKey} badge="RESOLVED" actions={<Link className="tool-button" href="/">← 案件一覧</Link>}/><ProductIdentity product={d}/><ProductHistory product={d}/></AppShell>}
