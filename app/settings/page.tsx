"use client";
import {useState} from "react";
import {AppShell} from "@/components/app-shell";
export default function SettingsPage(){const[capital,setCapital]=useState(300000);const[minProfit,setMinProfit]=useState(5000);const[confidence,setConfidence]=useState(95);return <AppShell capital={capital}><header className="page-head"><div><h1>研究設定</h1><p>ローカル検証の資金・判定基準・安全設定を確認します。</p></div><span className="env-badge">LOCAL ONLY</span></header><section className="settings-layout"><div className="settings-card"><h2>Paper Trading</h2><label><span>初期資金<small>仮想取引で使える上限</small></span><input type="number" value={capital} onChange={e=>setCapital(Number(e.target.value))}/></label><label><span>最低利益<small>BUY判定に必要な利益</small></span><input type="number" value={minProfit} onChange={e=>setMinProfit(Number(e.target.value))}/></label><label><span>商品同定の最低信頼度<small>弱いマッチを案件から除外</small></span><input type="number" value={confidence} onChange={e=>setConfidence(Number(e.target.value))}/></label><p className="save-note">現在は画面上のプレビューです。永続設定APIへの接続は次の実装対象です。</p></div><div className="settings-card"><h2>環境ガード</h2>{["ローカルの外部API接続","本番でのモック利用","CAPTCHA・ログイン回避","価格履歴の上書き"].map(x=><div className="guard-row" key={x}><span>{x}</span><b className="off">禁止</b></div>)}</div></section></AppShell>}
