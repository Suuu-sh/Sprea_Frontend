"use client";
import {useEffect,useState} from "react";import {AppShell} from "@/components/app-shell";import {StatCard,StatGrid} from "@/components/ui";import {getResearchDashboard,ResearchDashboard} from "@/lib/api";
const yen=(n:number)=>new Intl.NumberFormat("ja-JP",{style:"currency",currency:"JPY",maximumFractionDigits:0}).format(n);
export default function PaperTrading(){const[d,setD]=useState<ResearchDashboard|null>(null);useEffect(()=>{getResearchDashboard().then(setD)},[]);const p=d?.portfolio??{initialCapital:300000,lockedCapital:0,availableCash:300000,openTrades:0};return <AppShell capital={p.initialCapital} title="Paper Trading" description="実データから検出した案件だけを仮想取引します。" badge="PRODUCTION DATA"><StatGrid><StatCard label="利用可能資金" value={yen(p.availableCash)}/><StatCard label="拘束中" value={yen(p.lockedCapital)} help={`${p.openTrades}件`}/><StatCard label="初期資金" value={yen(p.initialCapital)}/></StatGrid><div className="empty-card">実データからBUY候補が検出されると、ここに取引状況が表示されます。</div></AppShell>}
